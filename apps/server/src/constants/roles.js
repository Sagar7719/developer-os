/**
 * Role Constants for Developer OS Identity Platform.
 * No raw role strings should be hardcoded elsewhere in the codebase.
 */
export const Roles = Object.freeze({
  ADMIN: 'ADMIN',
  USER: 'USER',
});

export default Roles;
